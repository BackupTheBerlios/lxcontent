<?php
	/**
	 * A Modul for generating a sitemap
	 */
	function generateOutput($xmlParser)
	{
/*		$files = readDirectoryRecursiv("content");
		
		$i=0;
		while ($i<count($files))
		{
			$ext = extractFileExt($files[$i]);
			if ((getPathElement($files[$i], -1)!="directory.xml") and (($ext=="html") or ($ext=="xml")))
			{
			}
			else
			{
				unset($files[$i]);
			}
			$i++;
		}
		
		for ($i=0; $i<count($files); $i++)
		{
			if (isset($files[$i]))
				$t .= $files[$i] . "<br />\n";
		}
*/
		return "Don't working yet ;-)";
	}
?>
